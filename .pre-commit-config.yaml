# AUTO-GENERATED by /opt/workspace-ci/scripts/scaffold-ci [2026-09-25T15:36:12Z]
# Source: ci-profile.yaml | tier=strict | languages=rust shell
# Re-generate (safe, pre-commit only): make -C /opt/workspace-ci scaffold-ci CONSUMER=. --force-precommit --yes
# WARNING: --force-all overwrites Makefile + config/*.
#          --force is removed; use --force-precommit | --force-makefile | --force-configs | --force-all.
repos:
  - repo: local
    hooks:
      - id: check-unstaged
        name: "Check Unstaged"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_unstaged'"
        language: system
        pass_filenames: false
        always_run: true
      - id: block-sensitive-files
        name: "Block Sensitive Files"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_block_sensitive_files'"
        language: system
        pass_filenames: false
        always_run: true
      - id: gitleaks
        name: "Gitleaks"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_scan_secrets'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-banned-words
        name: "Check Banned Words"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_banned_words'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-file-length
        name: "Check File Length"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_file_length'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-markdown-docs
        name: "Check Markdown Docs"
        entry: "uv run --project /opt/workspace-ci --no-sync python -m ci.check_markdown_docs"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-init-files
        name: "Check Init Files"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_init_files'"
        language: system
        pass_filenames: false
        always_run: true
      - id: ci-type-check
        name: "Ci Type Check"
        entry: "make type-check"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-policy-integrity
        name: "Check Policy Integrity"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_policy_integrity'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-dependency-versions
        name: "Check Dependency Versions"
        entry: "uv run --project /opt/workspace-ci --no-sync python -m ci.check_dependency_versions"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-py-not-executable
        name: "Check Py Not Executable"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_py_not_executable'"
        language: system
        pass_filenames: false
        always_run: true
      - id: ci-lint
        name: "Ci Lint"
        entry: "make lint"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-module-size
        name: "Check Module Size"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_module_size'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-npm-lock-sync
        name: "Check Npm Lock Sync"
        entry: "uv run --project /opt/workspace-ci --no-sync python -m ci.check_npm_lock_sync"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-portable-shell
        name: "Check Portable Shell"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_portable_shell'"
        language: system
        pass_filenames: false
        always_run: true
      - id: ci-check-error-swallow
        name: "Ci Check Error Swallow"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_silent_swallow'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-coverage-no-devolution
        name: "Check Coverage No Devolution"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_coverage_thresholds_no_devolution'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-required-hooks-present
        name: "Check Required Hooks Present"
        entry: "uv run --project /opt/workspace-ci --no-sync python -m ci.check_required_hooks_present"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-resolution-shapes
        name: "Check Resolution Shapes"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_fallback_resolution'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-commit-message
        name: "Check Commit Message"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_commit_message "$1"' --"
        language: system
        pass_filenames: false
        always_run: true
        stages: [commit-msg]
      - id: block-coauthored
        name: "Block Coauthored"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_block_coauthored "$1"' --"
        language: system
        pass_filenames: false
        always_run: true
        stages: [commit-msg]
      - id: ci-check-push
        name: "Ci Check Push"
        entry: "bash -c 'if [ "$(uname -s)" = Darwin ]; then make test-podman; else make check-push; fi'"
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
      - id: block-coauthored-history
        name: "Block Coauthored History"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_block_coauthored_history'"
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
      - id: cargo-fmt
        name: Cargo Format Check
        entry: "bash -c 'if [ \"$(uname -s)\" = Darwin ]; then echo \"SKIP: covered by make test-podman (Tier 1)\"; exit 0; fi; cargo fmt --all -- --check'"
        language: system
        pass_filenames: false
        always_run: true
        files: \.rs$

      - id: cargo-clippy
        name: Cargo Clippy
        entry: "bash -c 'if [ \"$(uname -s)\" = Darwin ]; then echo \"SKIP: covered by make test-podman (Tier 1)\"; exit 0; fi; cargo clippy --workspace --all-targets -- -D warnings'"
        language: system
        pass_filenames: false
        always_run: true
        files: \.rs$

      - id: verify-coverage
        name: Verify Test Coverage
        entry: "bash -c 'if [ \"$(uname -s)\" = Darwin ]; then echo \"SKIP: covered by make test-podman (Tier 1)\"; exit 0; fi; source /opt/workspace-ci/lib/checks.sh && ci_verify_coverage'"
        language: system
        stages: [pre-push]
        pass_filenames: false
