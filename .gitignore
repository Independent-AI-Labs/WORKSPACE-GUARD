.env
/target
/git-guard/target
/.bats-tmp

# Fleet-specific identity (real emails/names). Copy from *.example on each host.
config/home-lock-users.yaml
config/agent-git-identity
config/host-provision.yaml
# Regen stamp for `make gitleaks-ignore-regen` (per-host, regenerated)
.gitleaksignore.refs-sha256
